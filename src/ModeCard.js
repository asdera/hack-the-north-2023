import React from 'react';

function ModeCard(props) {
    // string, image, bool, func
    const { name, icon, credits, onClick } = props;

    return (
        <div></div>
    );
}

export default ModeCard